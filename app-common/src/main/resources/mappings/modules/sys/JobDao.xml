<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >  
<mapper namespace="com.eryansky.modules.sys.dao.JobDao">
    
    <select id="findJobList" resultType="com.eryansky.modules.sys.mapper.QuartzJobDetail">
		SELECT
			a.sched_name AS 'instanceName',
			a.job_name AS 'jobName',
			a.job_group AS 'jobGroup',
			a.job_class_name AS 'jobClassName',
			b.trigger_name AS 'triggerName',
			b.trigger_group AS 'triggerGroup',
			b.trigger_state AS 'triggerState',
			c.cron_expression AS 'cronExpression',
			c.time_zone_id AS 'timeZoneId',
			FROM_UNIXTIME(b.next_fire_time/1000,'%Y-%m-%d %H:%i:%S') AS 'nextFireTime',
			FROM_UNIXTIME(b.prev_fire_time/1000,'%Y-%m-%d %H:%i:%S') AS 'prevFireTime'
		FROM
			${sysPrefix}qrtz_job_details a
		INNER JOIN ${sysPrefix}qrtz_triggers b ON a.job_name = b.job_name AND a.job_group = b.job_group
		INNER JOIN ${sysPrefix}qrtz_cron_triggers c ON b.trigger_name = c.trigger_name AND b.trigger_group = c.trigger_group
		WHERE 1=1
		<if test="query !=null and query !=''" >
			AND (
			a.sched_name LIKE
			<choose>
				<when test="dbName == 'db2'">'%'||#{query}||'%'</when>
				<when test="dbName == 'mysql'">CONCAT('%', #{query}, '%')</when>
				<when test="dbName == 'oracle'">'%'||#{query}||'%'</when>
				<otherwise>'%'+#{query}+'%'</otherwise>
			</choose>
			OR a.job_name LIKE
			<choose>
				<when test="dbName == 'db2'">'%'||#{query}||'%'</when>
				<when test="dbName == 'mysql'">CONCAT('%', #{query}, '%')</when>
				<when test="dbName == 'oracle'">'%'||#{query}||'%'</when>
				<otherwise>'%'+#{query}+'%'</otherwise>
			</choose>
			OR a.cron_expression LIKE
			<choose>
				<when test="dbName == 'db2'">'%'||#{query}||'%'</when>
				<when test="dbName == 'mysql'">CONCAT('%', #{query}, '%')</when>
				<when test="dbName == 'oracle'">'%'||#{query}||'%'</when>
				<otherwise>'%'+#{query}+'%'</otherwise>
			</choose>
			)
		</if>
		<if test="jobState !=null and jobState !=''" >
			AND b.trigger_state = #{jobState}
		</if>
    </select>

</mapper>